<% @doctor.appointments.each do |appt| %>
<%= link_to appt.appointment_datetime.strftime("%B %-d, %Y")+" at "+appt.appointment_datetime.strftime("%I:%M"), appointment_path(appt.id)%> -
<%= link_to appt.patient.name, patient_path(appt.patient.id) %> <br>
<% end %>