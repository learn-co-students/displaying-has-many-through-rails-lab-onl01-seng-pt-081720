<h1><%= @doctor.name %> - <%= @doctor.department %></h1>

<h3>Appointments:</h3>

<ul>

    <% @doctor.appointments.each do |appointment| %>
        <li><%= appointment.readable_date %>, <%= link_to appointment.patient.name, patient_path(appointment.patient) %></li>
    <% end %>

</ul>