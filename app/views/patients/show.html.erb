<h1>My Appointments</h1>

<ul>

    <% @patient.appointments.each do |appointment| %>
        <li><%= appointment.readable_date %>, <%= link_to appointment.doctor.name, doctor_path(appointment.doctor) %></li>
    <% end %>

</ul>