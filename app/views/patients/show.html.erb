<h1><%= @patient.name %>'s Appointments </h1>

<ol>
    <% @appointments.each do |appt| %>
        <li><%= link_to "#{appt.doctor.name}", doctor_path(appt.doctor) %> - <%= appt.readable_datetime %></li>
    <% end %>
</ol>